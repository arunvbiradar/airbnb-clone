import express from "express";

// App config
const app = express();

// Middleware
app.use(express());

// DB Config

// API endpoint

// Listener
app.listen(5000, () => console.log("server started"));
